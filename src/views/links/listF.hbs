<div class="container p-4">
    <div class="row mt-2">
        <div class="col-md-10">
            <div class="row mt-1">
                <form action="">
                    <select class="form-control">
                        <option>Default select</option>
                        </select>
                </form>

            </div>

            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <h1 class="text-center text-muted">Relación de Fincas</h1>
                    </tr>
                    <tr class="text-center">
                        <th>#</th>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Ubicación</th>
                        <th>CC Proveedor</th>
                        <th>Acción</th>
                    </tr>
                </thead>
                <tbody>
                    {{!-- {{#if (links) { }} --}}
                        {{#each links }}
                            <tr>
                                <td class="text-right"> {{  @index }} </td>
                                <td class="text-right"> {{id}} </td>
                                <td> {{name}} </td>
                                <td> {{location}} </td>
                                <td class="text-right"> {{ccproveedor}} </td>
                                <td> 
                          <a href="/updateP/{{links.cc}}" class="btn btn-info">Update</a>
                          <a href="/deleteP/{{links.cc}}" class="btn btn-danger">Delete</a>
                        </td>
                            </tr>
                        {{/each}}
                    {{!-- {{/if } }} --}}
                                
                </tbody>
            </table>
        </div>
       
    </div>
</div>